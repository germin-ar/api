insert into garden.plant_catalog (scientific_name, description,slug_scientific_name )
values (:scientific_name,
        :description,
        :slug_scientific_name

        );

-- //TODO: agregar los demas atributos que va a necesitar este plant catalog.
